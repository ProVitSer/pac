[general]
enabled = yes
pretty=no
websocket_write_timeout=100
allowed_origins=*


[${ASTERISK_ARI_CALL_USER}]
type=user
password=${ASTERISK_ARI_CALL_PASSWORD}
password_format=plain
read_only=no

[${ASTERISK_ARI_ROUTE_USER}]
type=user
password=${ASTERISK_ARI_ROUTE_PASSWORD}
password_format=plain
read_only=no
