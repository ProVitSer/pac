export enum TrunkType {
    call = 'call',
    tts = 'tts',
    telegram = 'telegram',
}

export enum TrunkStatus {
    up = 'up',
    down = 'down',
}

export enum OriginateCallStatus {
    successful = 'successful',
    error = 'erro',
}
