export enum STTProviderType {
    // yandex = 'yandex',
    sber = 'sber',
}

export enum SttRecognizeStatus {
    done = 'done',
    inProgress = 'inProgress',
    error = 'error',
    completed = 'completed',
}
