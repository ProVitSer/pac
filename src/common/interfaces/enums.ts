export enum Role {
    User = 'User',
    Manager = 'Manager',
    Admin = 'Admin',
    API = 'Api',
}

export enum Permission {
    Delete = 'Delete',
    Create = 'Create',
    Update = 'Update',
    Read = 'Read',
}
