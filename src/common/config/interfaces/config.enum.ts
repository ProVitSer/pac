export enum NodeEnvType {
    prod = 'prod',
    development = 'development',
    // local = 'local',
    // test = 'test'
}

export enum AppProtocol {
    http = 'http',
    https = 'https',
}

export enum LogLevel {
    console = 'console',
    error = 'error',
    warn = 'warn',
    info = 'info',
    http = 'http',
    verbose = 'verbose',
    debug = 'debug',
    silly = 'silly',
}
