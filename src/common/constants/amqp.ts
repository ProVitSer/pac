export enum RoutingKey {
    mail = 'mail.sendMail',
    pbxCqa = 'pbx.cqa',
}

export enum Exchange {
    events = 'events',
}

export enum Queues {
    events = 'events',
    pbxCqaQueue = 'pbxCqaQueue',
}
